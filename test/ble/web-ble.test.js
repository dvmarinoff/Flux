import { uuids } from '../../src/ble/uuids.js';
import { ble, _ } from '../../src/ble/web-ble.js';

describe('request device', () => {
    test('', async function() {
        expect(1).toBe(1);
    });
});
